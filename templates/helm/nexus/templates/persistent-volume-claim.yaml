---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "nexus.fullname" . }}-pv-claim
  labels:
    app: {{ include "nexus.fullname" . }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: 5Gi

